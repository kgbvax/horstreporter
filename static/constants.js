export const CONSTANTS = {
    // Map defaults
    DEFAULT_MAP_CENTER: [20, 0],
    DEFAULT_MAP_ZOOM: 2,
    TILE_MAX_ZOOM: 18,

    // UI & Filter defaults
    DEFAULT_MAX_MINUTES: 25,
    DEFAULT_SSB_MIN_DB: 0,
    DEFAULT_CW_MIN_DB: -15,
    DEFAULT_CLUSTER_DIST_KM: 500,

    // Timings (ms)
    RENDER_THROTTLE_MS: 1000,
    STATUS_UPDATE_THROTTLE_MS: 750,
    SPOT_AGE_INTERVAL_MS: 5000,
    BAND_CYCLE_INTERVAL_MS: 3000,
    STATS_UPDATE_INTERVAL_MS: 10000,
    GEO_TIMEOUT_MS: 10000,
    GEO_MAX_AGE_MS: 60000
};